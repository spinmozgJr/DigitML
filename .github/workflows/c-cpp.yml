name: C/C++ CI

on:
  push:
    branches: [ "Konopkin_ks40" ]
  pull_request:
    branches: [ "Konopkin_ks40" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: installation
      run: sudo apt install -y googletest libgtest-dev build-essential gcovr
    - name: compill unit tests
      run: make test
    - name: run unit tests
      run: ./main
    - name: make standart activate function
      run: make 
    - name: run standart activate function
      run: ./main
    - name: make arctg tangent activate function
      run: make arctg
    - name: run arctg tangent activate function
      run: ./main
